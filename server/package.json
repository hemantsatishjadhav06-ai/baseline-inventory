{
  "name": "baseline-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Baseline AI proxy — keeps the OpenRouter key server-side.",
  "scripts": { "start": "node index.js" },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
